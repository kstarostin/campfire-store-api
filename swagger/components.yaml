components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: Use <i>/users/login</i> to get a JWT token.
  responses:
    unauthorizedError:
      description: Access token is missing or invalid.
