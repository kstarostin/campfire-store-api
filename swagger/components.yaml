components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: Test.
  responses:
    unauthorizedError:
      description: Access token is missing or invalid.
